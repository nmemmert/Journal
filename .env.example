# Copy to .env — all variables used by docker-compose.yml

# Required: sign JWT sessions — generate with: openssl rand -base64 32
JWT_SECRET=change-this-to-a-long-random-secret

# Public URL shown in the app (no trailing slash)
NEXT_PUBLIC_APP_URL=https://journal.example.com
